name: Keep FastAPI Awake

on:
  schedule:
    - cron: '*/10 * * * *'  # Runs every 10 minutes

jobs:
  ping-render:
    runs-on: ubuntu-latest
    steps:
      - name: Send HTTP Ping
        run: curl -s https://aahaar-be.onrender.com/health